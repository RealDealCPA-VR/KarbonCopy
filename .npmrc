# pnpm's default symlinked node_modules causes webpack to bundle Next.js's
# internal React context modules (e.g. LayoutRouterContext) more than once,
# which crashes the App Router client with
#   "invariant expected layout router to be mounted".
# A hoisted (flat) node_modules — like npm/yarn — resolves each module once.
node-linker=hoisted
